// Shared brand types used across data files and UI components.

// Accent color used for neon glows and category tags.
// Matches the GlassCard `glow` options so data maps straight to UI.
export type Accent = "blue" | "purple" | "gold";

// Lifecycle status shown on venture cards.
export type VentureStatus = "Live" | "In Development" | "Coming Soon";
